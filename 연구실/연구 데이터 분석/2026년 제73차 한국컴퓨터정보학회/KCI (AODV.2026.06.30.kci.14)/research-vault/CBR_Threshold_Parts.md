
**1. AODV 기반 RREP 전달 제어 구조**

- AODV에서 차량이 언제 RREP를 전달하는지
- `low CBR threshold`, `high CBR threshold`를 어떻게 적용하는지
- `local CBR`과 비교해서 전달 여부를 결정하는 방식

**2. 실험 데이터 수집 방식**

- 왜 `random` 방식으로 임계값 적용 데이터를 수집했는지
- 어떤 로그를 수집했는지
- RREP 판단 로그와 1초 진단 로그를 어떻게 대응시켰는지

**3. 학습 샘플 구성**

- 샘플 1개를 무엇으로 정의했는지
- 어떤 입력을 붙였는지
- 어떤 사후 결과를 붙였는지
- 1초 단위 매칭 방식

**4. 점수 계산 방식**

- `S, F, D, R, I, X`가 무엇인지
- 점수식을 어떻게 정의했는지
- 그 점수를 어디에 쓰는지

**5. 전처리 및 상태 구성**

- 결측 제거
- 홉 수 제한
- threshold 정수화
- `local CBR`, 이웃 차량 수, 홉 수, `hop<=1` 변수로 상태를 만드는 방식

**6. 상태별 정답지 생성**

- 상태 안에서 threshold 조합별 평균 점수 계산
- 최소 샘플 수 조건
- 최고 점수 대비 `0.2` 후보군
- 최종 대표 조합 선택 규칙

**7. ipynb 기반 학습 모델**

- 입력 4개, 출력 2개
- MLP 구조
- 표준화
- 손실 함수, optimizer, 조기 종료, 학습률 감소

**8. 평가 방법**

- train/valid/test run 구분
- 샘플 단위 평가
- 상태 평균 단위 평가
- `R²`, `MAE`

**9. AODV 적용 방식**

- 학습된 모델이 현재 차량 상태를 입력받아 임계값 예측
- 예측된 `low/high CBR threshold`와 현재 `local CBR` 비교
- RREP 전달 여부 결정